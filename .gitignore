# macOS
.DS_Store

# IDE workspace
*.code-workspace

# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
build/

# Database (privacy; do not commit user data)
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# Secrets and local config (do not commit)
.git-secret-replace.txt
ExportOptions.plist
build/ExportOptions.plist
Configuration.xcconfig
*.xcconfig
!*.xcconfig.example
!Default.xcconfig
.env
.env.*
!.env.example
*.pem
*.p12
*.key
id_rsa*
credentials/
secrets/
*.mobileprovision
*.provisionprofile
**/Auth*.swift
**/Secrets*.swift
**/APIKeys*.swift
*.keystore
*.jks

# Icon generator (Node)
scripts/icon_gen/node_modules/

# Generated App Store screenshots (keep local; upload to App Store Connect)
assets/screenshots/ipad-13-*.png
assets/screenshots/appstore/

scripts/resize_ipad_screenshot.sh

# Swift Package Manager (Packages/* and any local SPM)
.build/
**/.build/
.swiftpm/

# Xcode test and tooling
*.xcresult
*.xcresult bundle

# Coverage / test output (optional)
coverage/
*.gcno
*.gcda

# Internal docs / plans
docs/Phase4-XcodeCloud-CI.md

# fastlane / App Store Connect (never commit secrets)
AuthKey_*.p8
*.p8
app_store_connect_api_key.json
.xcode-cloud/
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output

# Bundler (keep Gemfile.lock committed for CI)
vendor/bundle/
.bundle/
